import React from 'react';
import {StyleSheet, View} from 'react-native';

type UserSignupScreenProps = {};

const UserSignupScreen = ({}: UserSignupScreenProps) => {
  return <View></View>;
};

const styles = StyleSheet.create({});

export default UserSignupScreen;
